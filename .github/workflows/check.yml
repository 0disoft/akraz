name: check

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  rust:
    strategy:
      matrix:
        include:
          - os: windows-latest
            cargo_scope: "--workspace"
          - os: ubuntu-latest
            cargo_scope: "--workspace --exclude akraz-app"
          - os: macos-latest
            cargo_scope: "--workspace --exclude akraz-app"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: cargo fmt --all -- --check
      - if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          cargo build -p akraz-daemon
          New-Item -ItemType Directory -Force apps/akraz/src-tauri/binaries
          Copy-Item target/debug/akraz-daemon.exe apps/akraz/src-tauri/binaries/akraz-daemon-x86_64-pc-windows-msvc.exe
      - run: cargo clippy ${{ matrix.cargo_scope }} --all-targets -- -D warnings
      - run: cargo test ${{ matrix.cargo_scope }}

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run test
      - run: bun run smoke:windows-mvp-release-gate
      - run: bun run smoke:windows-mvp-release-bundle
      - run: bun run check
      - run: bun run format:check
      - run: bun run lint

  smoke:
    needs:
      - rust
      - frontend
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run smoke:loopback-transport
      - run: bun run smoke:tcp-transport
      - run: bun run smoke:peer-session
      - run: bun run smoke:peer-session-executor
      - run: bun run smoke:remote-control-loopback
      - run: bun run smoke:runtime-recovery
      - run: bun run smoke:session-connect-lifecycle
      - run: bun run smoke:diagnostics-snapshot
      - run: bun run smoke:daemon-lifecycle
      - run: bun run smoke:settings-start
      - name: Run Windows MVP soak smoke
        shell: pwsh
        run: |
          bun run smoke:windows-mvp-soak -- --duration-ms 1 --max-cycles 1 --report-file reports/windows-mvp-soak-smoke.json
      - name: Upload Windows MVP soak report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: windows-mvp-soak-smoke-report
          path: apps/akraz/reports/windows-mvp-soak-smoke.json
          if-no-files-found: warn
          retention-days: 14
